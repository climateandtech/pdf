# Upgraded Docling for isolated benchmark venv only (install via setup_isolated_benchmark_env.sh).
# Do not -r requirements.txt here — production pins docling<2.43 and pip cannot resolve both.
docling>=2.43.0
