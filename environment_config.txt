# Environment Configuration Template
# Copy this to .env and fill in your actual values

# NATS Configuration
NATS_URL=nats://your-nats-server-ip:4222
NATS_TOKEN=your-secure-token-here-replace-with-random-64-char-string

# S3 Configuration for Hetzner Object Storage
AWS_ACCESS_KEY_ID=your-hetzner-access-key
AWS_SECRET_ACCESS_KEY=your-hetzner-secret-key
AWS_DEFAULT_REGION=fsn1
S3_ENDPOINT_URL=https://fsn1.your-objectstorage.com
S3_BUCKET_NAME=your-bucket-name

# Optional: Additional NATS settings
# NATS_CONNECT_TIMEOUT=10
# NATS_MAX_RECONNECT_ATTEMPTS=10

# Processing Configuration
DOCLING_TIMEOUT=600
USE_S3_STORAGE=true
DEBUG=true

# GPU memory profile (docling_worker.py / worker_runtime.py)
# full | a | nats  → 20gb_nats (path A: shared GPU with Ollama)
# capped_5gb | b | 5gb → ~5GB VRAM cap (path B benchmark)
DOCLING_GPU_PROFILE=full
# DOCLING_GPU_CAP_GB=5

# Optional: push worker.log to Loki when GPU is not on Coolify fluent-bit drain
# LOKI_PUSH_URL=https://your-loki/api/v1/push